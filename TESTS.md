Inventory Management System - Programmatic Tests

I included `InventoryTests.java` under `src/`. It contains several programmatic checks for the `InventoryStack` and `InventoryItem` behavior (push/peek/pop/getAll/isEmpty/size).

How to run (PowerShell):

```markdown
Inventory Management System - Programmatic Tests

I included `InventoryTests.java` under `src/test/java/`. It contains several programmatic checks for the `InventoryStack` and `InventoryItem` behavior (push/peek/pop/getAll/isEmpty/size).

How to run (PowerShell):

  cd "C:\Users\SAKETH\Documents\Project\Inventory Management System"
  javac -d bin src\main\java\*.java src\test\java\*.java; java -cp bin InventoryTests

The test program prints PASS/FAIL per test and exits with code 0 when all pass.
```
